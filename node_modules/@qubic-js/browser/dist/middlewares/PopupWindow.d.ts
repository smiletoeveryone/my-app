import { JsonRpcMiddleware } from 'json-rpc-engine';
import { ApiConfig, Messenger } from '@qubic-js/core';
import BrowserBridge from '../utils/BrowserBridge';
declare class PopupWindow implements Messenger {
    bridge: BrowserBridge;
    private apiConfig;
    private walletUrl;
    private isReady;
    private proxy;
    private newWindowReminderModal;
    constructor(walletUrl: string, apiConfig: ApiConfig);
    private getUrl;
    private detectCloseEventTimer;
    private detectPopupWindowCloseEvent;
    private openPopupWindow;
    hide: () => void;
    private show;
    private waitUntilReady;
    createPrepareBridgeMiddleware: () => JsonRpcMiddleware<unknown, unknown>;
}
export default PopupWindow;
