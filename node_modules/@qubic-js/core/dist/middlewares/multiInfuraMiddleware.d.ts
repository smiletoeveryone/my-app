import { JsonRpcMiddleware } from 'json-rpc-engine';
import { Network } from '../types';
import { Bridge } from '../types/bridge';
interface MultiInfuraMiddlewareOptions {
    initNetwork: Network;
    projectId: string;
}
export declare const createMultiInfuraMiddleware: (options: MultiInfuraMiddlewareOptions, bridge: Bridge) => JsonRpcMiddleware<unknown, unknown>;
export {};
