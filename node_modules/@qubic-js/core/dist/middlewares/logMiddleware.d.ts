import { JsonRpcMiddleware } from 'json-rpc-engine';
interface Options {
    label?: string;
}
declare const createLogMiddleware: (options?: Options | undefined) => JsonRpcMiddleware<unknown, unknown>;
export default createLogMiddleware;
